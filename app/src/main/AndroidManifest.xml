<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edward.carteraclientes">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/blanco"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Recaragar"></activity>
        <activity android:name=".Inicio" />
        <activity
            android:name=".Buscar"
            android:label="@string/title_activity_buscar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActNuevoCliente"
            android:label="@string/title_activity_act_nuevo_cliente"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActMain"
            android:label="@string/title_activity_act_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>